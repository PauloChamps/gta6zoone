(()=>{const open=document.querySelector('[data-menu-open]'),menu=document.querySelector('#mobile-menu'),overlay=document.querySelector('.mobile-overlay'),close=document.querySelector('[data-menu-close]');if(!open||!menu||!overlay)return;let previous;const toggle=state=>{menu.classList.toggle('open',state);overlay.classList.toggle('open',state);document.body.classList.toggle('menu-open',state);open.setAttribute('aria-expanded',String(state));menu.setAttribute('aria-hidden',String(!state));menu.toggleAttribute('inert',!state);if(state){previous=document.activeElement;close?.focus()}else previous?.focus()};open.addEventListener('click',()=>toggle(true));close?.addEventListener('click',()=>toggle(false));overlay.addEventListener('click',()=>toggle(false));document.addEventListener('keydown',event=>{if(event.key==='Escape'){if(menu.classList.contains('open'))toggle(false);document.querySelectorAll('.dropdown>button[aria-expanded="true"]').forEach(button=>{button.setAttribute('aria-expanded','false');button.parentElement.classList.remove('keyboard-open');button.focus()})}if(event.key==='Tab'&&menu.classList.contains('open')){const items=[...menu.querySelectorAll('a,button')],first=items[0],last=items.at(-1);if(event.shiftKey&&document.activeElement===first){event.preventDefault();last.focus()}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus()}}});menu.querySelectorAll('.submenu-toggle').forEach(button=>button.addEventListener('click',()=>{const submenu=document.getElementById(button.getAttribute('aria-controls')),state=!submenu.classList.contains('open');submenu.classList.toggle('open',state);button.setAttribute('aria-expanded',String(state))}));document.querySelectorAll('.dropdown>button').forEach(button=>{const dropdown=button.parentElement;button.addEventListener('click',()=>{const state=button.getAttribute('aria-expanded')!=='true';document.querySelectorAll('.dropdown>button').forEach(other=>{if(other!==button){other.setAttribute('aria-expanded','false');other.parentElement.classList.remove('keyboard-open')}});button.setAttribute('aria-expanded',String(state));dropdown.classList.toggle('keyboard-open',state)});dropdown.addEventListener('mouseenter',()=>button.setAttribute('aria-expanded','true'));dropdown.addEventListener('mouseleave',()=>{if(!dropdown.classList.contains('keyboard-open'))button.setAttribute('aria-expanded','false')});dropdown.addEventListener('focusout',event=>{if(!dropdown.contains(event.relatedTarget)){button.setAttribute('aria-expanded','false');dropdown.classList.remove('keyboard-open')}})})})();
