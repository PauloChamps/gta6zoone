import { intro,image,badge,gallery,youtube } from './common.mjs';
import { canonicalFor } from './base.mjs';
export default function noticia(entry,root){const slug=`noticias/${entry.slug}`,body=`${intro({title:entry.titulo,description:entry.subtitulo||entry.resumo},root,[{label:'Notícias',slug:'noticias'},{label:entry.titulo}])}<article class="section"><div class="container article">${badge(entry.status)}<p class="meta">${entry.data||'Data a definir'}${entry.autor?` · ${entry.autor}`:''}</p>${image(entry,root,'noticias','article-image',true)}${(entry.conteudo||[]).map(section=>`<h2>${section.titulo}</h2><p>${section.texto}</p>`).join('')}${gallery(entry.galeria,root,'noticias')}${youtube(entry.youtube,`Vídeo: ${entry.titulo}`)}${entry.fontes?.length?`<h2>Fontes e referências</h2><ul>${entry.fontes.map(source=>`<li>${source}</li>`).join('')}</ul>`:''}</div></article>`,schemas=[{'@context':'https://schema.org','@type':'BreadcrumbList','itemListElement':[['','Início'],['noticias','Notícias'],[slug,entry.titulo]].map(([path,name],index)=>({'@type':'ListItem',position:index+1,name,item:canonicalFor(path)}))}];if(entry.data&&entry.autor)schemas.push({'@context':'https://schema.org','@type':'NewsArticle',headline:entry.titulo,datePublished:entry.data,author:{'@type':'Organization',name:entry.autor},mainEntityOfPage:canonicalFor(slug)});return {slug,title:entry.titulo,description:entry.resumo,ogType:'article',body,schemas};}
